import React from 'react';
import GlobalLoading from './GlobalLoading';

export default {
  component: GlobalLoading,
  title: 'GlobalLoading',
};

const Template = (args) => <GlobalLoading {...args} />;

export const Default = Template.bind({});
